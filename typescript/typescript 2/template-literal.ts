(function () {
    const nombre = 'Marco';
    const apellido = 'Pérez';
    const edad = 19;

    // const salida = nombre + apellido + edad;
    const salida = ` ${nombre}\n ${apellido}\n (${edad})`;
    console.log(salida);
})();